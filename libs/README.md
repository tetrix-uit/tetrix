# Libraries

This directory contains the shared libraries and the public libraries of the project.

Each library must have its own directory. Make a library for code that more than one
application or service needs. Also make a library for a package that you publish for other users.

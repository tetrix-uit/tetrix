# Deployment

This directory contains the deployment configuration and the related files.

If the deployment code has a separate release process or a separate access process, keep it
in its own directory. Give the target environment and the deployment steps in the documentation.

# Applications

This directory contains the frontend applications.

Each application must have its own directory. Keep the user interface, the frontend code,
and the documentation of the application in that directory.

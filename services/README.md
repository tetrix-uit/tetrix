# Services

This directory contains the server-side services.

Each service must have its own directory. Keep the service code, the API reference,
and the documentation of the service in that directory.
